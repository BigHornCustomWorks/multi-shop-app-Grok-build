/** Product branding */
export const APP_NAME = 'Custom Shop Management';

/**
 * Only this email can open Master Control and manage all shops.
 * Compared case-insensitively after login.
 */
export const PLATFORM_ADMIN_EMAIL = 'bighorncustomworks@gmail.com';

/** localStorage key for Firebase web config JSON */
export const FIREBASE_CONFIG_KEY = 'csm_firebase_config';

/** Max logo / photo size (bytes) */
export const MAX_IMAGE_BYTES = 5 * 1024 * 1024;
